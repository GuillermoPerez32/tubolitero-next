export const API_HOST = "https://tuboliteros.com";
export const API_URL = "/api";

export enum Endpoints {
  apkInfo = `${API_HOST}${API_URL}/apk/1`,
  lotteries = `${API_HOST}${API_URL}/loterias`,
}
